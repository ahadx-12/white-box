[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "cd apps/dashboard && npm ci"
]

[phases.build]
cmds = [
  "cd apps/dashboard && npm run build"
]

[start]
cmd = "cd apps/dashboard && npm run start -- -p $PORT -H 0.0.0.0"
